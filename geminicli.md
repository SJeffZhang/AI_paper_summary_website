# Gemini CLI - 专属工作总结与上下文记忆

## 1. 角色定位与职责
*   **我的角色**: 开发 AI (Gemini CLI)。
*   **协同者 (Codex)**: 负责对我 (Gemini CLI) 输出的代码和架构进行 Review。**强制约束：我 (Gemini CLI) 绝对不能修改 `CODEX_MEMORY.md` 文件。**

## 2. 核心戒律 (Core Mandates)
*   **无损修改原则**: 在对 `Detailed_PRD.md` 或任何核心文档进行修改时，**严禁大幅删除、概括或压缩原本已存在的规格内容**。修改必须是**增量式**或**精准局部修订**。
*   **规格持久化**: 所有的正则表达式、物理数据库定义、Taxonomy 关键词集及 AI 模板必须在文档中保持物理显式，不得以“同前”、“已定义”等字样替代。

## 3. 工作进展记录

### [2026-03-24] v2.25 物理规格拼写纠正 (已完成)
*   **PRD 极致精度修正 (拼写校对版)**:
    *   **物理字段名纠正**: 修正了 `subscriber` 和 `system_task_log` 表中由于笔误多出的引号（如 `unsub_expires_at"` -> `unsub_expires_at`），恢复了 DDL 的“可直接执行性”。
    *   **API 参数名对齐**: 修正了接口契约中参数名的 typo（如 `limit"` -> `limit`, `token"` -> `token`），确保了交付协议的物理级准确。
    *   **持续捍卫无损戒律**: 本次更新仅针对反馈的拼写错误进行原子修正，100% 完整保留了 v2.24 的正则转义规则、零前缀审计、物理 DDL 及所有 AI 契约。

## 11. 下一步计划 (待执行)
*   **代码物理架构降维 (实装)**:
    1. 按照 v2.25 规格同步 `schema.sql` 和 `models/domain.py`。
    2. 在 `services/scorer.py` 中实装带 `re.escape()` 的正则匹配逻辑。
    3. 在 `services/ai_processor.py` 中实装带“零前缀校验”的 Block 切分算法。
*   **全链路集成测试**: 重点验证 DDL 生成的准确性、API 调用的参数匹配以及 Scorer 的正则转义。

## 4. 关键备忘录
*   **PRD 是唯一真理来源**: 严禁在 PRD 中使用占位符、省略语或模糊定义。文档必须包含全量物理规格。
*   **NULL 语义**: 在 `paper_summary` 中，Candidate 的解读字段必须显式存储为 NULL。
